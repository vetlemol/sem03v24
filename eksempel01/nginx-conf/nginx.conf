server {
    listen 80;
    listen [::]:80;
    server_host localhost;

    location / {
        root /var/www/html;
        index index.html;
    }
    location /50x.html {
        root /usr/share/nginx/html;
    }
}